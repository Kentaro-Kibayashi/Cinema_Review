<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% @users.each do |user| %>

<%# refile %>
<%# fallback(何も画像が設定されていない際に) "no-image.png" を表示 %>
<%= attachment_image_tag user, :profile_image, fallback: "no-image.png" %>
<%= user.username %>
<% end %>